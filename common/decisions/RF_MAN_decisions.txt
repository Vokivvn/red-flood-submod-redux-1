MAN_equipment_sending = {
     MAN_japanese_shipment = { 
		days_re_enable = 1
		days_remove = 1
		is_good = yes                       
        selectable_mission = no               
        days_mission_timeout = 29  
		fire_only_once = no                  
        allowed = { tag = MAN }
		visible = {
			is_subject_of = JAP
		}                
        available = {                           
			is_subject = no             
        }
        activation = {                          
             is_subject_of = JAP                    
        }           
        timeout_effect = {                     
			JAP = { country_event = { id = japan.600 } } 
        }
        complete_effect = {                    
			
			}                
        }
     }